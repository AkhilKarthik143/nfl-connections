const socket = io(); let state = null, selfId = null, local = null, selected = null;
const app = document.querySelector('#app');
const esc = (s = '') => String(s).replace(/[&<>"']/g, (c) => ({ '&':'&amp;', '<':'&lt;', '>':'&gt;', '"':'&quot;', "'":'&#39;' }[c]));
const norm = (s) => String(s).toLowerCase().replace(/[^a-z0-9]/g, '');
const jerseys = (p) => (Array.isArray(p.number) ? p.number : [p.number]).filter(Boolean).map(String);
function landing(error = '') { local = null; selected = null; app.innerHTML = `<section class="game hero"><div class="eyebrow">NFL CHAIN GAME</div><h1>NFL CONNECTIONS</h1><p>The AI chooses a team. Name a player from it, then build a chain by matching teams, jersey numbers, or colleges. Three strikes and you're out.</p><div class="form"><button id="local">Pass &amp; play</button><button class="secondary" id="online">Play online</button><div class="error">${esc(error)}</div></div></section>`; document.querySelector('#local').onclick = localSetup; document.querySelector('#online').onclick = onlineLanding; }
function onlineLanding(error = '') { app.innerHTML = `<section class="game hero"><div class="eyebrow">MULTIPLAYER CHAIN GAME</div><h1>PLAY ONLINE</h1><p>Create a room and share its code. Rooms support 2–6 players.</p><div class="form"><input id="name" maxlength="16" placeholder="Your name"><button id="create">Create room</button><input id="code" maxlength="4" placeholder="Room code"><button class="secondary" id="join">Join room</button><button class="secondary" id="back">Back</button><div class="error">${esc(error)}</div></div></section>`; document.querySelector('#create').onclick = () => socket.emit('create-room', { name: document.querySelector('#name').value }); document.querySelector('#join').onclick = () => socket.emit('join-room', { name: document.querySelector('#name').value, code: document.querySelector('#code').value }); document.querySelector('#back').onclick = landing; }
function render() { if (local) return localRender(); if (!state) return onlineLanding(); if (state.phase === 'lobby') return lobby(); if (state.phase === 'finished') return finish(state.winner, state.chain.length); game(); }
function lobby() { const host = state.hostId === selfId; app.innerHTML = `<section class="game hero"><div class="eyebrow">YOUR ROOM CODE</div><div class="code">${state.code}</div><h2>THE HUDDLE (${state.players.length}/6)</h2><p>${state.players.map((p) => esc(p.name)).join(' · ')}</p><p>Have everyone open this site, enter the code, then join the room.</p>${host ? '<button id="start">Start game</button>' : '<p>Waiting for the host to start…</p>'}</section>`; document.querySelector('#start')?.addEventListener('click', () => socket.emit('start-game')); }
function finish(winner, length) { app.innerHTML = `<section class="game end"><div class="eyebrow">FINAL WHISTLE</div><h1>${esc(winner || 'Nobody')} WINS</h1><p>The chain reached ${length} players.</p><button id="again">Play again</button><button class="secondary" id="menu">Modes</button></section>`; document.querySelector('#again').onclick = local ? localSetup : onlineLanding; document.querySelector('#menu').onclick = landing; }
function playersMarkup(players, turn) { return `<div class="players">${players.map((p, i) => { const active = (p.id || i) === turn; return `<div class="player ${active ? 'active' : ''} ${p.out ? 'out' : ''}"><b>${esc(p.name)}</b><span class="flags">${'■'.repeat(p.strikes)}${'□'.repeat(3 - p.strikes)}</span><small>${p.out ? 'OUT' : active ? 'ON THE CLOCK' : 'waiting'}</small></div>`; }).join('')}</div>`; }
function cards(chain) { return chain.length ? `<div class="chain">${chain.map((c) => `<div class="card"><b>${esc(c.name)}</b><small>${esc(c.team)} · ${esc(c.connector)} · named by ${esc(c.by)}</small></div>`).join('')}</div>` : ''; }
function moveForm(step, team, submit) { const connection = step === 'connection'; return `${connection ? `<div class="moves"><button class="secondary move ${selected === 'TEAM' ? 'selected' : ''}" data-type="TEAM">Same team</button><button class="secondary move ${selected === 'NUMBER' ? 'selected' : ''}" data-type="NUMBER">Same number</button><button class="secondary move ${selected === 'COLLEGE' ? 'selected' : ''}" data-type="COLLEGE">Same college</button></div>` : ''}<div class="submit"><input id="move" placeholder="${step === 'team-player' ? `Player who played for ${esc(team)}` : 'New player name'}" ${connection && !selected ? 'disabled' : ''}><button id="submit" ${connection && !selected ? 'disabled' : ''}>Submit</button></div>`; }
function bindMove(handler) { document.querySelectorAll('[data-type]').forEach((b) => b.onclick = () => { selected = b.dataset.type; render(); }); const submit = () => { const value = document.querySelector('#move')?.value.trim(); if (value) handler(value); }; document.querySelector('#submit')?.addEventListener('click', submit); document.querySelector('#move')?.addEventListener('keydown', (e) => e.key === 'Enter' && submit()); }
function game() { const me = state.players.find((p) => p.id === selfId), mine = state.turnId === selfId; app.innerHTML = `<section class="game"><header class="header"><div><div class="eyebrow">CHAIN GAME · ROOM ${state.code}</div><div class="title">CONNECTIONS</div></div><div class="chip" style="--team:${state.team.color}">${esc(state.team.name).toUpperCase()}</div></header>${playersMarkup(state.players, state.turnId)}<div class="status">${esc(state.message)}</div>${cards(state.chain)}<footer class="foot">${mine ? moveForm(state.step, state.team.name) : `<div class="status">${me?.out ? 'You are out of this round.' : 'Wait for your turn.'}</div>`}</footer></section>`; if (mine) bindMove((name) => { const event = state.step === 'team-player' ? 'submit-team-player' : 'submit-connection'; socket.emit(event, state.step === 'team-player' ? { name } : { name, type: selected }); selected = null; }); }
function localSetup() { app.innerHTML = `<section class="game hero"><div class="eyebrow">PASS &amp; PLAY</div><h1>SAME DEVICE</h1><p>Add 2–6 players, then hand the device to the next player after each move.</p><div class="form" id="names"><input maxlength="16" placeholder="Player 1"><input maxlength="16" placeholder="Player 2"></div><div class="form"><button class="secondary" id="add">Add player</button><button id="kick">Kick off</button><button class="secondary" id="back">Back</button><div class="error" id="local-error"></div></div></section>`; document.querySelector('#add').onclick = () => { const inputs = document.querySelectorAll('#names input'); if (inputs.length < 6) { const input = document.createElement('input'); input.maxLength = 16; input.placeholder = `Player ${inputs.length + 1}`; document.querySelector('#names').append(input); } }; document.querySelector('#back').onclick = landing; document.querySelector('#kick').onclick = async () => { try { const pool = await fetch('/api/players').then((r) => r.json()); if (!pool.length) throw Error(); const names = [...document.querySelectorAll('#names input')].map((i, n) => i.value.trim() || `Player ${n + 1}`); const seed = pool[Math.floor(Math.random() * pool.length)], team = seed.teams[Math.floor(Math.random() * seed.teams.length)]; local = { pool, names, strikes: names.map(() => 0), out: names.map(() => false), turn: 0, team, step: 'team-player', chain: [], used: new Set(), message: `AI picked the ${team}. ${names[0]}, name a player who played for that team.`, phase: 'game' }; localRender(); } catch { document.querySelector('#local-error').textContent = 'Could not load the player pool. Try again.'; } }; }
function nextLocal() { const alive = local.out.map((out, i) => !out ? i : -1).filter((i) => i >= 0); if (alive.length <= 1) { local.phase = 'over'; return; } for (let step = 1; step <= local.names.length; step++) { const candidate = (local.turn + step) % local.names.length; if (!local.out[candidate]) { local.turn = candidate; return; } } }
function localStrike(reason) { const index = local.turn, name = local.names[index]; local.strikes[index]++; if (local.strikes[index] >= 3) local.out[index] = true; nextLocal(); local.message = local.phase === 'over' || local.out[index] ? `${name} is out — three strikes.` : `${name}: strike ${local.strikes[index]}/3. ${reason}`; localRender(); }
function localRender() { if (local.phase === 'over') return finish(local.names.find((_, i) => !local.out[i]), local.chain.length); const players = local.names.map((name, i) => ({ id: i, name, strikes: local.strikes[i], out: local.out[i] })); app.innerHTML = `<section class="game"><header class="header"><div><div class="eyebrow">PASS &amp; PLAY</div><div class="title">CONNECTIONS</div></div><div class="chip">${esc(local.team).toUpperCase()}</div></header>${playersMarkup(players, local.turn)}<div class="status">${esc(local.message)}</div>${cards(local.chain)}<footer class="foot">${moveForm(local.step, local.team)}</footer></section>`; bindMove(localSubmit); }
function localSubmit(name) { const candidate = local.pool.find((p) => norm(p.name) === norm(name)); if (local.step === 'team-player') { if (!candidate || !candidate.teams.includes(local.team)) return localStrike(`${name || 'That player'} did not play for the ${local.team}.`); if (local.used.has(norm(candidate.name))) return localStrike(`${candidate.name} is already in the chain.`); local.chain.push({ name: candidate.name, team: local.team, by: local.names[local.turn], connector: `Team: ${local.team}` }); local.used.add(norm(candidate.name)); local.step = 'connection'; nextLocal(); local.message = `Correct: ${candidate.name}. ${local.names[local.turn]}, connect a player by team, jersey number, or college.`; return localRender(); } const anchor = local.pool.find((p) => norm(p.name) === norm(local.chain.at(-1).name)); let shared; if (candidate && !local.used.has(norm(candidate.name))) { if (selected === 'TEAM') shared = candidate.teams.find((t) => anchor.teams.includes(t)); if (selected === 'NUMBER') shared = jerseys(candidate).find((n) => jerseys(anchor).includes(n)); if (selected === 'COLLEGE' && candidate.college && candidate.college === anchor.college) shared = candidate.college; } if (!shared) return localStrike(`${name || 'That player'} has no matching ${String(selected || '').toLowerCase()} connection with ${anchor.name}.`); const connector = selected === 'TEAM' ? `Team: ${shared}` : selected === 'NUMBER' ? `#${shared}` : `College: ${shared}`; local.chain.push({ name: candidate.name, team: candidate.teams.at(-1), by: local.names[local.turn], connector }); local.used.add(norm(candidate.name)); nextLocal(); local.message = `Connection holds: ${connector}. ${local.names[local.turn]}, you're up.`; selected = null; localRender(); }
socket.on('joined', ({ id }) => { selfId = id; }); socket.on('state', (s) => { state = s; render(); }); socket.on('error-message', onlineLanding); landing();
